#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    void matchPairs(int n, char nuts[], char bolts[])
    {
        // code here
        sort(nuts, nuts + n);
        sort(bolts, bolts + n);
    }
};

int main()
{
    return 0;
}